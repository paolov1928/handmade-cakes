import React from "react"
import styled, { css } from "styled-components"

const Title = styled.h3`
  color: white;
  margin: 1vh;
  font-family: cursive;
`

const About = () => {
  return <Title>I am Effie the best baker in the whole of the 7 kingdoms</Title>
}

export default About
